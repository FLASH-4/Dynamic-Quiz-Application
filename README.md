# Dynamic Quiz Application

## Folders
- `Frontend/` → HTML, CSS, JS code of the quiz app
- `Automation/` → Selenium automation code using Java + Maven

## How to Run
1. Start Live Server in `frontend/` folder to run the quiz.
2. Run `QuizAutomationTest.java` from `selenium-tests/src/test/java/com/quiz/tests/`.
